# StorageInternalRead

/ = / script "StorageRead"

/data = read {
  model: /model,
  id: /id,
  selections: /selections
}

/ = expandReferences {
  model: /model,
  data: /data,
  selections: /selections
}
