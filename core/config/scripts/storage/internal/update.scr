# StorageInternalUpdate

/ = / script "StorageUpdate"

/data = update {
  id: /id,
  data: /input,
  model: /model
}

/ = expandReferences {
  model: /model,
  data: /data,
  selections: /selections
}
