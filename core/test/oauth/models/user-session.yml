id: UserSession
description: User session token
store: true
properties:
  token:
    type: string
  user:
    $ref: '#/definitions/User'
  expires:
    type: integer
  created:
    type: integer
required:
  - token
  - user
  - expires
  - created
