database: rethink
access: 'true'
mutation: 'q("rank").rank >= i.rank'
required:
  - title
  - body
properties:
  title:
    type: string
    maxLength: 255
  body:
    type: string
  rank:
    type: integer
    default: '1'
