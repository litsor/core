database: rethink
access: 'i.id === u.id'
mutation: 'i.id === u.id'
required:
- name
- mail
properties:
  name:
    type: string
    maxLength: 255
  mail:
    type: string
    maxLength: 255
    indexed: true
  badges:
    type: array
    items:
      type: string
