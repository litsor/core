id: Update
steps:

- if:
    left: /model
    right: User
    then:
      - if:
          left: /input/password
          then:
            - hash-password:
                _output: /input/password
                password: /input/password

- update:
    id: /id
    data: /input
    model: /model
