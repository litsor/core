id: Create
steps:

- if:
    left: /model
    right: User
    then:
      - if:
          left: /input/password
          then:
            - hash-password:
                _output: /input/password
                password: /input/password

- create:
    data: /input
    model: /model

